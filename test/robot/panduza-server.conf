server {
    listen      80;
    listen      [::]:80;

    server_name  localhost www.panduza.fr panduza.fr;

    root   /home;
    index  report.html;

    location /images/ {
        alias /home;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
