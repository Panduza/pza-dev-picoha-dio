server {
    listen      80;
    listen      [::]:80;

    server_name  localhost www.panduza.fr panduza.fr;

    root   /home/www;
    index  index.html;

    location /images/ {
        root /home;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
